# Full public base URL of the Bitrix D7 API, without a trailing slash.
# Example: https://cms.example.ru
BITRIX_API_URL=

# Local development only. Production/CI builds must provide BITRIX_API_URL.
BITRIX_ALLOW_EMPTY_SNAPSHOT=0
